// import { validationResult } from "express-validator";

// export const throwValidationResult = (req, res, next) => {
//     const result = validationResult(req);
//     if (!result.isEmpty()){
//         const errors = result.array
//         throw new ValidationError(errors)
//     }
//     next();
// }

